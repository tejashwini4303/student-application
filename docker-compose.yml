services:

  postgres:
    image: postgres:16

    environment:
      POSTGRES_DB: studentdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

  springboot:
    build: .

    ports:
      - "8080:8080"

    depends_on:
      - postgres